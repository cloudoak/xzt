package com.ambition.agile.modules.jzmk.show;

/**
 * 测评结果公用类
 * @author wyz
 * @version 2017-11-17
 */
public class TestView {
	/*protected int rid;
    protected SqlConnection con;
    protected void Draw()
    {
        
    }

    protected void Page_Load(object sender, EventArgs e)
    {
        rid = int.Parse(Request.QueryString["rid"]);
        if (!IsPostBack)
        {
            con = new SqlConnection(Global.DB_CON);
            con.Open();
            Draw();
            con.Close();
        }
    }

    protected int get_exid()
    {
        return Convert.ToInt32(Tools.ExecuteScalar(con, "SELECT score2 FROM test_score WHERE gid=0 AND rid=" + rid));
    }

    protected int get_total_score()
    {
        return Convert.ToInt32(Tools.ExecuteScalar(con, "SELECT score FROM test_score WHERE gid=0 AND rid=" + rid));
    }*/
}
